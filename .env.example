# 第一次运行：复制为 .env 后至少填写 LLM_API_KEY，再在应用「设置 → 数据源」配置财报接口。
# Research Canvas — environment variables (copy to .env)
# 更完整的启动说明与可选变量见 example/startup/env.example

# Server (API backend — internal, proxied by Vite)
STOCK_RESEARCH_PORT=8711
STOCK_RESEARCH_HOST=127.0.0.1
# 局域网：STOCK_RESEARCH_HOST=0.0.0.0

# Web (user-facing)
# Dev/preview: http://127.0.0.1:5173
WEB_PORT=5173
# 局域网：WEB_HOST=0.0.0.0

# LLM (DeepSeek / OpenAI-compatible)
LLM_PROVIDER=DeepSeek
LLM_MODEL=deepseek-chat
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com

# Defaults
DEFAULT_SCORECARD=综合评估
DEFAULT_TOP_N=20

# Optional: Vite dev proxy target (client-ui only)
# VITE_API_BASE=/api

# Optional: user data directory (default ~/.research-canvas)
# RESEARCH_CANVAS_DATA_DIR=
# OPPTRIX_DATA_DIR=

# Optional: local owner auth
# OPPTRIX_AUTH_SAFE_MODE=1
# OPPTRIX_TRUSTED_PROXIES=127.0.0.1,::1,10.0.0.0/8
# OPPTRIX_TRUSTED_LOCAL_CIDRS=
# OPPTRIX_AUTH_COOKIE_SECURE=1
# OPPTRIX_AUTH_KEY_PATH=

# CodeGraph (local code index — optional; telemetry off by default after `codegraph telemetry off`)
# CODEGRAPH_TELEMETRY=0
