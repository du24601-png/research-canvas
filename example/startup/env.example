# Research Canvas 启动环境变量示例
# 用法：cp example/startup/env.example .env  然后填入密钥

# ── API 服务（内部端口，Web 开发时由 Vite 代理 /api）──
STOCK_RESEARCH_PORT=8711
STOCK_RESEARCH_HOST=127.0.0.1
# 局域网：STOCK_RESEARCH_HOST=0.0.0.0

# ── Web 前端（开发 / 预览）──
WEB_PORT=5173
# 局域网：WEB_HOST=0.0.0.0

# ── LLM（OpenAI 兼容接口，对话功能需要）──
LLM_PROVIDER=DeepSeek
LLM_MODEL=deepseek-chat
LLM_API_KEY=
LLM_BASE_URL=https://api.deepseek.com

# ── 默认投研参数 ──
DEFAULT_SCORECARD=综合评估
DEFAULT_TOP_N=20

# ── 可选：Tushare Pro（也可在设置 → 数据源 中配置）──
# TUSHARE_TOKEN=
# 非官方服务地址时填写，默认 https://api.tushare.pro
# TUSHARE_HTTP_URL=

# ── 可选：独立用户数据目录（试用 example 配置时推荐）──
# RESEARCH_CANVAS_DATA_DIR=/path/to/your/research-canvas-data
# OPPTRIX_DATA_DIR=/path/to/your/data  # 旧名，仍兼容

# ── 可选：桌面壳层标记（一般由 Electron 自动设置）──
# OPPTRIX_DESKTOP=1

# ── 可选：本地账户（未创建时可完成首次配置；创建后须登录）──
# OPPTRIX_AUTH_SAFE_MODE=1
# OPPTRIX_TRUSTED_PROXIES=127.0.0.1,::1,10.0.0.0/8
# OPPTRIX_AUTH_COOKIE_SECURE=1

# ── 可选：Vite 开发代理 ──
# VITE_API_BASE=/api
