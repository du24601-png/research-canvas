# 新闻订阅导入示例

`subscriptions.example.json` 符合应用内「导出 / 导入」使用的 `schema_version: 1` 格式。

## 导入步骤

1. 启动 Opptrix  
2. 打开 **设置 → 新闻订阅**  
3. 使用 **导入** 选择本文件  
4. 导入后可在设置中启用/禁用单条订阅并触发刷新  

RSS 源可用性会随第三方站点变化；若某条失败，可在设置中删除或更换 URL。
